"use strict";
function calcArea(base, altura) {
    return base * altura;
}
function somar(...number) {
    //number.reduce();
    console.log(number);
}
function test() {
    if (10 > 5) {
        return 'Verdadeiro';
    }
    else {
        return 5;
    }
}
const areaCalc = (base, altura) => base * altura;
const result = test();
